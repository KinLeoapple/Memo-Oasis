package org.kinleoapple.plugins.database.dao

import org.kinleoapple.plugins.database.Database

fun postBlog(database: Database, json: String, id: Long?): Map<String, String?> {
    return mapOf("saved" to "")
}